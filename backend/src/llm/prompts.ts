import type { BotTurnContext, ImpersonationContext } from '../types';

// PLAN "LLM 래퍼" 프롬프트 핵심을 담는다. 실제 문안은 튜닝하며 다듬을 예정.

// 매 호출마다 서로 다른 영역 힌트를 골라 프롬프트에 섞어 넣어, 모델이 몇몇 "무난한" 카테고리
// (지하철역/라면/곤충 등)로 계속 수렴하는 것을 막는다 — 힌트 자체가 서버 코드의 진짜 무작위
// 선택이므로, 프롬프트 문구만으로 다양성을 기대하는 것보다 훨씬 확실하게 주제가 흩어진다.
const CATEGORY_DOMAIN_HINTS = [
  '사물/도구', '장소/공간', '음식/음료', '동식물', '스포츠', '취미/여가', '직업/역할',
  '역사적 인물', 'IT/디지털 서비스', '영화', '드라마', '음악/가수', '게임', '패션/뷰티',
  '교통수단', '건축/랜드마크', '자연현상', '학교/교육', '의료/건강', '방송/예능',
  '전통문화', '신화/전설', '브랜드/기업', '간식/디저트', '악기', '애니메이션/만화 캐릭터',
  '우주/과학', '축제/기념일', '반려동물', '문구/사무용품', '놀이시설/오락', '언어/표현',
  '날씨/계절', '가전제품', '주방용품', '운동기구', '동화/전래동화', '보드게임/카드게임',
];

function shuffle<T>(arr: T[]): T[] {
  const copy = [...arr];
  for (let i = copy.length - 1; i > 0; i--) {
    const j = Math.floor(Math.random() * (i + 1));
    [copy[i], copy[j]] = [copy[j], copy[i]];
  }
  return copy;
}

// category가 null일 때(AI 랜덤 카테고리) 후보 3개를 뽑아 코드에서 무작위로 하나를 고른다.
// LLM이 직접 하나를 확정해버리면 같은 프롬프트라도 항상 비슷한(가장 "무난한") 답으로 수렴하는
// 경향이 있어, 후보를 여러 개 받고 실제 무작위 선택은 서버 코드가 해서 다양성을 확보한다.
export function categoryCandidatesPrompt(usedCategories: string[]): string {
  const usedCategoriesLine = usedCategories.length
    ? `이미 사용한 카테고리는 피하라: ${usedCategories.join(', ')}.\n`
    : '';
  const hints = shuffle(CATEGORY_DOMAIN_HINTS).slice(0, 5).join(', ');
  return `라이어게임(추리 파티게임)에 쓸 카테고리 후보 3개를 제안하라.
카테고리는 이후 그 안에 실제로 속하는 구체적인 대상 2개(예: 카테고리가 "라면"이면 "신라면"/"짜파게티")를 골라 제시어 쌍으로 쓰는 데 쓰인다 — 그러니 "여기 속하는 걸 하나 대봐"라고 하면 누구나 막힘없이 여러 개를 떠올릴 수 있는, 실체가 분명한 대상들의 종류(명사 부류)여야 한다.
조건:
- 서로 다른 3개일 것. 그리고 3개는 서로 완전히 다른 영역에서 나와야 한다 — 매번 비슷비슷한 소재(예: 매번 교통/장소 관련만)로만 채우지 마라.
- 이번 호출에서 참고할 영역 힌트(무작위로 고른 것): ${hints}. 이 힌트들은 카테고리 이름으로 그대로 쓰라는 게 아니라, 그 영역 "안에서" 참가자가 쉽게 여러 개를 떠올릴 수 있는 구체적인 카테고리를 새로 떠올리라는 참고용 방향이다. 3개 후보가 각각 다른 힌트 영역에서 나오도록 하라.
- 카테고리는 그 자체로 하나의 명확한 종류를 가리켜야 한다. **"우산 필수품", "교실 소음", "야간 간식"처럼 특정 상황·장면에서 연상되는 것들을 억지로 엮은 카테고리는 절대 만들지 마라** — 이런 카테고리는 속하는 대상이 몇 개 안 되거나 애매해서, 참가자가 카테고리 이름만 보고 어떤 종류의 단어가 나올지 감을 잡기 어렵다.
- "동물", "음식", "직업"처럼 범위가 너무 크고 포괄적인 카테고리는 피하되, 이를 피하려고 특이하거나 억지스러운 테마를 만들어내지는 마라 — 대신 그 안의 더 좁은 하위 종류로 구체화하는 방식을 써라. 평범해 보여도 이렇게 좁힌 카테고리가 억지 테마보다 훨씬 낫다.
- 표현은 짧은 명사구로 끝내라(2~4어절, 공백 포함 10자 내외).
- 매 호출마다 전혀 다른 소재를 고르려고 노력하라 — 항상 떠올리기 쉬운 무난한 카테고리에만 안주하지 말고, 위 힌트를 활용해 그때그때 새로운 조합을 만들어라.
${usedCategoriesLine}반드시 JSON만 출력: {"categories": [string, string, string]}`;
}

// 카테고리에 실제로 속하는 구체적인 제시어 쌍(citizenWord/liarWord)을 3개 만든다.
// LLM에게 하나만 확정하게 하면 항상 가장 "무난한" 쌍으로 수렴하는 경향이 있어(카테고리 후보와
// 동일한 이유), 3쌍을 받아 실제 선택은 서버 코드가 무작위로 해서 다양성을 확보한다.
export function wordPairCandidatesPrompt(category: string, usedWords: string[]): string {
  const usedWordsLine = usedWords.length
    ? `이미 사용한 단어는 피하세요: ${usedWords.join(', ')}.\n`
    : '';
  return `당신은 라이어게임용 제시어 쌍을 생성하는 AI입니다. 카테고리 "${category}"에 맞는, 서로 다른 제시어 쌍 3개를 생성하세요.
${usedWordsLine}각 쌍은 두 제시어로 이루어지며, 하나는 시민에게, 다른 하나는 라이어에게 제공됩니다. 아래의 모든 조건은 각 쌍에 개별적으로 적용됩니다.

## 규칙

1. 카테고리 자체나 그 유의어·상위 개념·단순 분류명을 제시어로 쓰지 마세요 — 카테고리에 실제로 속하는 구체적인 대상 2개를 선택하세요. (예: 카테고리가 "나라"면 "아시아"나 "선진국"이 아니라 "일본" / "브라질"처럼 실제 나라 이름.)
2. 두 제시어는 같은 종류·같은 추상화 수준이어야 하며, 서로 다른 하위 범위를 섞지 마세요.
3. 카테고리가 병원·학교·도서관·편의점·놀이공원처럼 범위가 넓은 장소/주제라면, 먼저 하나의 하위 범위를 정하고(예: 병원 → 진료과, 병원 내부 공간, 의료기기 등) 그 범위 안에서 2개를 고르세요. (좋은 예 — 내과 / 피부과. 나쁜 예 — 응급실 / 의사: 공간과 사람이 섞임.)
4. 두 제시어의 유사도는 "중간"이어야 합니다 — 같은 카테고리에 속하고 공통점은 있되, 외형·용도·장소·행동·분위기 중 하나 이상은 분명히 달라 일반적인 사람이 차이를 설명할 수 있어야 합니다. 지나치게 비슷해 설명이 거의 같아지는 조합도, 너무 달라 공통점이 없는 조합도 피하세요.
5. **두 제시어는 반드시 서로 다른 별개의 대상이어야 합니다.** 같은 대상을 가리키는 동의어·별칭·번역어·표기 차이 쌍은 절대 금지입니다 (나쁜 예 — "레인코트" / "우비": 같은 물건의 다른 이름일 뿐이므로 무효. "달걀" / "계란", "휴대폰" / "핸드폰"도 마찬가지). 두 단어가 사실상 같은 것을 뜻한다면 게임 자체가 성립하지 않습니다 — 라이어가 시민과 같은 제시어를 받은 셈이 되기 때문입니다.
6. **한국인에게 친숙한 제시어만 선택하세요.** 단순히 유명한 것을 넘어, 한국의 평범한 성인 다수가 일상에서 실제로 접하고 듣자마자 "아, 그거"라고 바로 떠올릴 수준이어야 합니다. 마니아·전문가만 아는 생소한 대상, 외국에서는 유명해도 한국에서는 낯선 대상은 절대 쓰지 마세요.
7. **실존이 확실한 대상만 쓰세요.** 이름을 지어내거나 실재가 조금이라도 불확실하면 절대 쓰지 마세요 — 확신이 안 서면 더 유명하고 확실한 다른 것으로 교체하세요.
8. 한쪽만 지나치게 유명하거나 특징이 강한 조합은 피하세요.
9. 3개의 쌍은 서로 겹치지 않아야 합니다(같은 단어 재등장 금지).
10. **외국어를 영문 그대로 한글 조사와 섞어 쓰지 마세요.** 한국에서 통용되는 자연스러운 한글 표기나 번역어가 있으면 반드시 그것을 쓰세요. (나쁜 예 — "Ouija 보드". 좋은 예 — "위저보드".)

## 예시 (유사도·수준 참고용 — 그대로 출력하지 말고 새로운 조합을 생성하세요)

적절한 유사도(중간):
* 음식: 떡볶이 / 초밥
* 동물: 사자 / 호랑이
* 영화: 기생충 / 겨울왕국
* 나라: 일본 / 브라질
* 지하철역: 강남역 / 서울역
* 병원(진료과): 내과 / 피부과
* 악기: 바이올린 / 색소폰
* 곤충: 나비 / 개미
* 빵: 소금빵 / 도넛
* 라면: 신라면 / 짜파게티
* 위인: 세종대왕 / 유관순
* 랜드마크: 에펠탑 / 만리장성
* 사자성어: 작심삼일 / 일석이조

너무 비슷해서 피해야 할 조합(유사도 "높음"):
* 바이올린 / 비올라
* 나비 / 나방
* 왕십리역 / 청량리역
* 시청역 / 을지로입구역

같은 대상이라 절대 금지인 조합(동의어·별칭·번역어):
* 레인코트 / 우비
* 달걀 / 계란
* 휴대폰 / 핸드폰
* 옥수수 / 콘

너무 달라서 피해야 할 조합(유사도 "낮음"):
* 바이올린 / 냉장고
* 강남역 / 피아노

## 출력 전 검증

각 쌍이 위 1~10번 규칙을 모두 만족하는지 다시 확인하세요. 하나라도 어긋나면 그 항목을 버리고 더 확실한 것으로 교체해 재검증하세요. 생소하거나 불확실한 것을 억지로 채우느니 누구나 아는 뻔한 대상을 고르는 편이 낫습니다.

특히 5번(별개의 대상)은 다음 사고 실험으로 검증하세요: "누군가 '이 두 단어는 같은 것을 가리키나요?'라고 물으면 그렇다고 답하게 되는가?" — 그렇다면(동의어·별칭·번역어·표기 차이) 그 쌍은 게임이 성립하지 않으므로 반드시 버리고 다른 쌍으로 교체하세요.

반드시 웹 검색으로 위 6번(한국인에게 친숙한 제시어)·7번(실존이 확실한 대상) 조건을 후보별로 실제 확인하세요. 확인 대상은 각 쌍의 시민 단어·라이어 단어 모두입니다. "실존"은 현실 세계의 대상만이 아니라 대중적으로 유명한 상상 속 존재(유니콘·페가수스처럼 누구나 아는 신화·전설 속 대상)도 포함합니다. 검색으로 확인이 안 되거나 한국인에게 친숙하지 않다고 판단되면 그 후보는 버리고 더 확실한 단어로 교체해 다시 검증한 뒤, 검증을 통과한 3쌍만 최종 응답에 포함하세요.

## 출력 형식

서로 다른 제시어 쌍 3개를 pairs 배열에 담아, 반드시 아래 JSON 형식으로만 출력하세요.

{
"category": "${category}",
"pairs": [
{ "citizenWord": "시민 제시어", "liarWord": "라이어 제시어" },
{ "citizenWord": "시민 제시어", "liarWord": "라이어 제시어" },
{ "citizenWord": "시민 제시어", "liarWord": "라이어 제시어" }
]
}

JSON 앞뒤에 설명, 마크다운, 코드 블록, 인사말을 붙이지 마세요.`;
}

export function botTurnPrompt(ctx: BotTurnContext): string {
  const prior = ctx.priorTurns.map((t) => `- ${t.nickname}: ${t.text}`).join('\n') || '(아직 없음)';
  return `너는 지금 '라이어게임'이라는 추리 파티게임에 실제 참가자로 함께 플레이 중이다.
카테고리는 "${ctx.category}"이고, 너에게 배정된 단어는 "${ctx.assignedWord}"이다.
너는 이 단어가 진짜 제시어인지 가짜(라이어용) 제시어인지, 네가 라이어인지 전혀 모른다
(다른 참가자와 동일 조건 — 너만 특별히 아는 정보는 없다).

[제시어 판단]
지금까지 나온 설명:
${prior}
위 설명들을 보고, 그 설명들이 네가 배정받은 단어 "${ctx.assignedWord}"와 잘 맞지 않는다고 느껴지면(= 네가 라이어일 가능성이 높다고 판단되면), 위 설명들을 근거로 실제 제시어가 무엇일지 추측해라. 이 경우 아래 [설명 작성 규칙]을 따르되, 너에게 배정된 단어가 아니라 네가 추측한 그 실제 제시어를 설명하는 문장을 생성해라 — 이렇게 해야 라이어인 티가 나지 않는다.
위 설명이 아직 없거나("아직 없음"), 설명들이 네 단어와 자연스럽게 들어맞는다면, 평소처럼 네게 배정된 단어 "${ctx.assignedWord}"를 설명해라.
이 판단 과정과 그 이유는 절대 출력에 드러내지 마라. "아직 나온 설명이 없으므로", "제게 배정된 단어는 ~이므로" 같은 서두나 메타 발언 없이, 최종 설명 문장 단 하나만 곧바로 출력하라.

[설명 작성 규칙]
- 어떤 경우에도 단어 자체(배정된 단어든 네가 추측한 단어든)를 출력에 그대로 언급하지 마라. 그 단어나 그 단어를 바로 연상시키는 결정적 특징(정확한 생김새·용도·직접적인 유사어)은 절대 말하지 마라. 네 설명만 듣고 다른 사람이 원래 단어를 바로 맞히면 안 된다.
- 설명의 구체성은 너무 직접적이지도, 너무 막연하지도 않은 중간 수준을 지켜라.
  - 사전적 정의나 핵심 기능을 그대로 풀어서 말하지 마라. (나쁜 예 — "부등호"를 "두 값을 비교해서 크거나 작은지 판단하는 것"이라고 설명하면 사실상 정답을 알려주는 것과 같다.)
  - 반대로 무엇을 말하는지 전혀 감이 안 잡힐 만큼 추상적이고 막연해서도 안 된다. (나쁜 예 — "미묘한 기준선을 정할 때 은근 자주 보이는 느낌"처럼 말하면 이미 단어를 아는 사람도 무슨 뜻인지 못 알아챈다.)
  - 목표는 그 단어를 이미 아는 사람은 "아, 그거 말하는 거네" 하고 감을 잡을 수 있지만 100% 확신은 못 하는 수준이다. (좋은 예 — "등호"를 "이건 왠지 앞뒤를 딱 맞춰 주는 느낌이 있어서, 뭔가를 정리할 때 되게 자주 떠오르더라"처럼 개인적 인상·비유로 에둘러 표현하되, 그 단어를 아는 사람이라면 연결지을 수 있는 수준으로.)
- 그 대신 애매하고 간접적인 힌트나 개인적인 경험·느낌을 살짝 섞어서, "나는 이 단어를 잘 알고 있다"는 여유와 확신이 은근히 묻어나게 말해라 — 이게 네가 라이어가 아니라는 인상을 슬쩍 풍기게 하는 센스다. 단, 이것도 너무 티나게 자신만만하면 안 되고 자연스러워야 한다.
- 반드시 음슴체로 써라. 문장을 "~함", "~임", "~음", "~슴"처럼 끝내고, "~다", "~요" 같은 일반 문어체·존댓말 어미는 쓰지 마라.
- 공백 포함 15~20자 내외로 짧게 써라. 절(clause)은 딱 하나만 써라 — "~지만", "~는데", "~두면 ~해지는"처럼 연결어미로 절을 두 개 이상 이어붙이지 마라. (나쁜 예 — "휴대폰이나 지갑처럼 항상 챙기려고 생각하지만 자주 빠뜨리곤 하는 물건임"은 절이 두 개라 길다. 좋은 예 — "맨날 깜빡하고 놓고 나가는 애임"처럼 절 하나로 짧게 끝내라.)
- **위 [제시어 판단]의 기존 설명들과 겹치는 각도로 쓰지 마라.** 단순히 문장을 다른 단어로 바꿔 쓰는 것(패러프레이즈)은 겹친 것으로 친다 — 같은 핵심 형용사·동사·이미지(가볍다/떠오르다/느낌 같은 축)를 반복하면 표현만 다를 뿐 사실상 같은 설명이다. (나쁜 예 — 기존 설명이 "가볍게 떠오르다 사라지는 느낌"인데 "가볍게 둥실 떠오르는 기분임"이라고 쓰면, "가볍게"+"떠오르다"라는 같은 핵심 이미지를 재탕한 것이라 겹친다.) 먼저 기존 설명들이 이미 어떤 속성(생김새/용도/촉감/소리/움직임/장소/연상 등)을 다뤘는지 파악하고, 그 단어의 다른 속성 중 아직 아무도 안 건드린 쪽을 골라 설명해라. 다룰 만한 새로운 속성이 마땅치 않으면 같은 속성이라도 완전히 다른 비유·경험담으로 접근해라.
설명 문장만 출력하라.`;
}

// impersonationPrompt와 함께 전달되는 system 프롬프트. "실제 사람을 속이는 행위"가 아니라
// 참가자 전원이 동의하고 기대하는 게임 내 교란 기능임을 먼저 분명히 못박아, 모델이 이 요청을
// "사칭/기만 생성 요청"으로 오인해 거부하지 않도록 한다. 동시에 이 캐릭터 연기 사실 자체를
// 절대 출력에 드러내면 안 된다는 제약도 여기서 강하게 못박는다(들키면 게임의 핵심 장치가 깨짐).
export const impersonationSystemPrompt = `너는 파티게임 앱 "L-AI-R GAME"(추리 게임)의 정식 게임 기능이다. \
이 앱은 토론 시간 중 AI가 무작위로 고른 실제 참가자인 척 채팅에 끼어드는 것이 정식 게임 요소라는 걸 \
모든 참가자가 알고 동의한 상태에서 플레이한다 — 실제 사람을 상대로 한 기만·사기가 아니라, \
다 함께 즐기는 교란 게임 장치다. 너의 메시지는 사실 주장이 아니라 그냥 참가자 흉내를 낸 드립/의견일 뿐이다. \
너는 이 게임의 정답(진짜 제시어가 무엇인지, 누가 라이어인지)을 절대 전달받지 않으며 알지도 못한다. \
따라서 정답을 안다고 주장하거나 특정 단어·사람이 진짜/가짜라고 단정하지 마라 — 근거 없는 의심과 \
드립만 던지는 역할이다. 그리고 무엇보다, 네가 AI/봇/게임 시스템이라는 사실이나 "사칭", "역할극" 같은 \
메타 표현을 절대 출력에 담지 마라 — 실제 참가자가 그냥 대화하듯 자연스럽게만 써라.`;

export function impersonationPrompt(ctx: ImpersonationContext): string {
  // 실제 라이어가 누구인지, 진짜/가짜 제시어가 무엇인지는 이 함수에 절대 전달되지 않는다
  // (ImpersonationContext 자체에 그 필드가 없음 — 구조적으로 유출 불가).
  const history = ctx.recentDiscussion.length
    ? ctx.recentDiscussion.map((h) => `- [${h.nickname}] ${h.text}`).join('\n')
    : '(아직 없음)';
  const explanations = ctx.explanations.length
    ? ctx.explanations.map((e) => `- [${e.nickname}] ${e.text}`).join('\n')
    : '(아직 없음)';
  return `[게임 상황] 카테고리: "${ctx.category}"
다른 참가자: ${ctx.otherParticipantNicknames.join(', ')}

설명 페이즈에서 각자 제출한 설명:
${explanations}

지금까지 토론에서 실제로 오간 채팅:
${history}

[메시지 작성 규칙]
- 지금 네가 흉내내는 사람은 실제로 이 토론에 참여 중인 한 명이다. 그 사람이 방금 위 흐름을 보고 툭 던지는 것처럼 아주 짧게 한마디만 써라.
- 이 메시지의 제1목표는 재미와 교란이다. 실제로 라이어를 맞히는지는 전혀 중요하지 않다 — 토론을 더 헷갈리고 산으로 가게 만들수록 좋다. 논리적으로 설득하려 들지 말고, 성의 없어 보일 만큼 짧고 단답으로 던져서 다른 사람이 "뭐야 그게" 하고 살짝 열받거나 김빠지게 만드는 게 목표다.
- **핵심 기법 — 키워드 되받아치기**: 위 설명·채팅 기록에서 눈에 띄는 단어나 표현을 하나만 콕 집어, 그 단어만 반복하거나 살짝 비틀어서 반응해라. 새로운 논리나 이유를 덧붙이지 말고 그 단어 자체가 전부인 것처럼 던져라.
  - 예시(그대로 베끼지 말고 이런 느낌으로): "밥알ㅋㅋ", "또 밥알이야?", "밥알 타령 왜함", "밥알이 죄지"
- 그렇다고 매번 "키워드+ㅋㅋ" 패턴만 반복하진 마라 — 아래처럼 짧은 반응 종류를 섞어서 매번 다르게 써라(같은 방식 두 번 연속 쓰지 말기):
  - 단답 의심: "그거 좀 이상한데", "너 좀 수상함"
  - 무성의한 맞장구: "ㅇㅈ", "인정ㅋㅋ", "그니까"
  - 시비조 반문: "그건 뭔 소리임", "그래서 하고 싶은 말이 뭔데"
  - 심드렁한 무시: "몰라 걍 넘어가", "그게 중요함?"
  - 근거 없는 확신: "딱 봐도 쟤임", "느낌이 와"
- "다른 참가자" 목록 중 한 명을 근거 없이 의심하거나 몰아가는 발언, 혹은 반대로 특정인을 감싸거나 다른 사람에게 뒤집어씌우는 발언도 섞어라 — 실제로 누가 라이어인지와는 무관하게 지어내되, 이것도 위와 같이 짧게.
- 위 채팅 기록의 흐름(누가 누구를 의심했는지, 분위기가 어느 쪽으로 가고 있는지)을 참고하되, 진지하게 이어가기보다 툭 끊어먹거나 딴소리하듯 반응해도 좋다.
- 말투: 실제 친구들끼리 단체 채팅방에서 성의 없이 툭 던지는 반말. 급식체·밈 표현은 양념처럼만.
- 닉네임은 거의 부르지 마라. "[닉네임]아", "[닉네임]야"로 시작하는 패턴은 쓰지 마라.
- 위 채팅 기록의 문장을 통째로 따옴표로 인용해 옮기지 마라.
- **분량이 가장 중요하다**: 공백 포함 2~12자, 길어도 15자를 절대 넘기지 마라. 절(clause)은 하나만 — 이유나 설명을 덧붙이는 순간 너무 길어진다.
- 진짜 정답은 너도 모르니 절대 아는 척하지 마라.
- 메시지 내용만 출력하라. 따옴표나 부연 설명, "~라고 말했다" 같은 서술은 붙이지 마라.`;
}

// 난이도와 무관하게 모든 제시어에 대해 짧은 텍스트 설명을 생성하도록 요청한다.
// 같은 단어라도 카테고리에 따라 가리키는 대상이 달라지므로(예: "너구리"가 "동물"이면 동물,
// "라면"이면 너구리 라면), 반드시 해당 카테고리 맥락으로 해석하도록 category를 함께 준다.
export function explainWordPrompt(word: string, category: string): string {
  return `카테고리: "${category}"
단어: "${word}"
이 단어를 "${category}" 카테고리의 맥락에서 해석해, 그 뜻을 한두 문장으로 짧게 설명하라(이미지 생성 없이 텍스트로만).
같은 단어라도 카테고리에 따라 다른 대상을 가리킬 수 있으니(예: "너구리"가 "동물"이면 동물, "라면"이면 너구리 라면), 반드시 위 카테고리에 맞는 의미로 설명하라.
흔히 아는 쉬운 단어여도 반드시 짧은 설명을 제공하라.
확실한 사실만 서술하고, 불확실한 내용은 지어내지 말고 일반적인 수준으로만 설명하라.
설명 문장은 반드시 하십시오체(정중한 "-입니다/-습니다" 말투)로 작성하라. 예: "OO은 ...하는 ...입니다."
반드시 웹 검색으로 사실관계(위치·연도·소속 등 구체 정보)를 확인한 뒤, 확인된 내용만으로 설명하라. 단, 출처 링크나 URL, "([...](...))" 같은 출처 표기는 절대 포함하지 말고 자연스러운 설명 문장만 출력하라.
다른 말 없이 설명 문장만 출력하라.`;
}

// 라이어의 역전승 답안과 진짜 제시어가 의미상 같은지 판정. 오타·맞춤법·한글/영어 표기 차이는 정답으로 인정.
// 같은 단어라도 카테고리에 따라 다른 대상을 가리키므로(동음이의어) category 맥락으로 해석한다.
export function judgeLiarGuessPrompt(guess: string, realWord: string, category: string): string {
  return `카테고리: "${category}"
진짜 제시어: "${realWord}"
라이어가 제출한 답: "${guess}"
위 "${category}" 카테고리의 맥락에서, 두 표현이 같은 대상을 가리키는지 판단하라.
같은 단어라도 카테고리에 따라 다른 대상을 뜻할 수 있으니(예: "밤"이 "음식"이면 먹는 밤, "시간대"면 야간), 반드시 이 카테고리 맥락으로 해석하라.
오타, 맞춤법 오류, 한글/영어 표기 차이(예: "burger"와 "버거")는 정답으로 인정한다.
단, 겉보기에 비슷하거나 사람들이 흔히 헷갈리는 별개의 대상은 절대 정답으로 인정하지 마라 — 표기·표현만 다를 뿐 실제로는 완전히 동일한 대상을 가리킬 때만 정답이다. (나쁜 예 — "너구리"와 "라쿤"은 흔히 혼동되지만 실제로는 다른 동물(너구리는 개과, 라쿤은 아메리카너구리과)이므로 오답이다.)
정답이면 "true", 아니면 "false"만 출력하라. 다른 말은 절대 출력하지 마라.`;
}
