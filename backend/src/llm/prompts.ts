import type { BotTurnContext, TurnCommentContext } from '../types';

// PLAN "LLM 래퍼" 프롬프트 핵심을 담는다. 실제 문안은 튜닝하며 다듬을 예정.

// category가 null일 때(AI 랜덤 카테고리) 후보 3개를 뽑아 코드에서 무작위로 하나를 고른다.
// LLM이 직접 하나를 확정해버리면 같은 프롬프트라도 항상 비슷한(가장 "무난한") 답으로 수렴하는
// 경향이 있어, 후보를 여러 개 받고 실제 무작위 선택은 서버 코드가 해서 다양성을 확보한다.
export function categoryCandidatesPrompt(usedCategories: string[]): string {
  return [
    '라이어게임(추리 파티게임)에 쓸 카테고리 후보 3개를 제안하라.',
    '조건:',
    '- 서로 다른 3개일 것.',
    '- "동물", "음식", "직업"처럼 너무 흔하고 뻔한 카테고리는 피하라.',
    '- 그렇다고 대부분의 사람이 못 알아들을 정도로 생소한 카테고리도 피하라 — 초중고생도 듣자마자',
    '  감이 오는 수준에서, 약간 의외성 있고 흥미로운 카테고리를 골라라.',
    usedCategories.length ? `이미 사용한 카테고리는 피하라: ${usedCategories.join(', ')}.` : '',
    '반드시 JSON만 출력: {"categories": [string, string, string]}',
  ]
    .filter(Boolean)
    .join('\n');
}

// 카테고리에 실제로 속하는 구체적인 제시어 쌍(citizenWord/liarWord) 1개를 만든다.
export function wordPairPrompt(category: string, usedWords: string[]): string {
  const usedWordsLine = usedWords.length
    ? `\n이미 사용한 단어는 피하세요: ${usedWords.join(', ')}.\n`
    : '';
  return `당신은 라이어게임용 제시어 쌍을 생성하는 AI입니다.

사용자가 입력한 카테고리에 맞는 제시어 2개를 생성하세요.

한 제시어는 시민에게 제공되고, 다른 제시어는 라이어에게 제공됩니다.

## 핵심 목표

두 제시어는 반드시 같은 카테고리에 속해야 합니다.

하지만 서로 지나치게 비슷해서 설명이 거의 동일해지는 조합은 피하세요.

좋은 제시어 쌍은 다음과 같습니다.

* 같은 카테고리에 속한다.
* 서로 어느 정도 공통점이 있다.
* 외형, 용도, 장소, 행동, 분위기, 특징 중 하나 이상이 분명하게 다르다.
* 일반적인 사람이 두 제시어의 차이를 설명할 수 있다.
* 라이어가 상대 제시어를 어느 정도 추측할 수는 있지만, 완전히 동일하게 설명하기는 어렵다.
* 시민과 라이어 어느 한쪽이 지나치게 유리하지 않다.

두 제시어의 유사도는 낮음, 중간, 높음 중 반드시 "중간" 수준이어야 합니다.

## 가장 중요한 규칙

1. 사용자가 입력한 카테고리 자체를 제시어로 사용하지 마세요.
2. 카테고리의 유의어, 상위 개념, 단순한 분류명을 제시어로 사용하지 마세요.
3. 해당 카테고리에 실제로 속하는 구체적인 대상 2개를 선택하세요.
4. 두 제시어는 같은 종류와 같은 추상화 수준이어야 합니다.
5. 서로 다른 하위 범위의 단어를 섞지 마세요.
6. 두 제시어가 지나치게 비슷한 세부 종류인 경우 사용하지 마세요.
7. 두 제시어가 서로 너무 달라 공통 설명이 거의 불가능한 경우도 사용하지 마세요.
8. 일반적인 사용자가 알고 설명할 수 있는 단어를 선택하세요.
9. 존재하지 않거나 사실 여부가 불확실한 고유명사를 만들지 마세요.
10. 한쪽만 지나치게 유명하거나 특징이 강한 조합은 피하세요.
11. 같은 제시어 쌍을 반복해서 생성하지 마세요.
12. 출력은 반드시 지정된 JSON 형식만 사용하세요.

## 카테고리 해석 규칙

카테고리는 제시어가 속해야 하는 집합입니다.

예를 들어 카테고리가 "지하철역"이라면 실제 지하철역 이름을 생성해야 합니다.

올바른 예시:

* 강남역 / 서울역
* 잠실역 / 홍대입구역
* 명동역 / 고속터미널역

잘못된 예시:

* 전철역 / 기차역
* 환승역 / 종착역
* 지하철 / 버스

카테고리가 "나라"라면 실제 나라 이름을 생성하세요.

올바른 예시:

* 일본 / 브라질
* 프랑스 / 인도
* 캐나다 / 이집트

잘못된 예시:

* 아시아 / 유럽
* 선진국 / 개발도상국
* 국가 / 도시

## 범위가 넓은 카테고리 처리

카테고리가 병원, 학교, 도서관, 편의점, 놀이공원처럼 넓은 장소나 주제인 경우에는 먼저 하나의 하위 범위를 선택하세요.

그다음 반드시 같은 하위 범위에 속하는 제시어 2개를 생성하세요.

예시:

카테고리: 병원

가능한 하위 범위:

* 병원 내부 공간
* 진료과
* 검사
* 의료기기
* 의료용품
* 병원 종사자

올바른 예시:

* 응급실 / 재활치료실
* 내과 / 피부과
* 엑스레이 / 혈액검사

잘못된 예시:

* 응급실 / 의사
* 내과 / 주사기
* 간호사 / MRI

카테고리: 학교

가능한 하위 범위:

* 학교 내부 공간
* 학교 구성원
* 학교 과목
* 학교에서 사용하는 물건

올바른 예시:

* 교실 / 운동장
* 급식실 / 음악실
* 보건실 / 도서실

잘못된 예시:

* 교실 / 선생님
* 수학 / 칠판
* 학생 / 운동장

## 유사도 조절 규칙

두 제시어는 같은 카테고리에 속해야 하지만, 너무 비슷한 조합은 피하세요.

피해야 할 조합:

* 바이올린 / 비올라
* 나비 / 나방
* 신라면 / 진라면
* 왕십리역 / 청량리역
* 소금빵 / 모닝빵
* 사슴벌레 / 장수풍뎅이
* 시청역 / 을지로입구역

이 조합들은 서로 차이가 너무 작아 설명이 거의 동일해질 가능성이 높습니다.

적절한 조합:

* 바이올린 / 색소폰
* 나비 / 개미
* 신라면 / 짜파게티
* 강남역 / 서울역
* 소금빵 / 도넛
* 장수풍뎅이 / 잠자리
* 잠실역 / 홍대입구역

너무 다른 조합도 피하세요.

피해야 할 조합:

* 바이올린 / 냉장고
* 나비 / 상어
* 강남역 / 피아노
* 신라면 / 축구

## 카테고리별 예시

* 음식: 떡볶이 / 초밥
* 영화: 기생충 / 겨울왕국
* 스포츠: 축구 / 수영
* 직업: 경찰관 / 요리사
* 나라: 일본 / 브라질
* 해양동물: 돌고래 / 불가사리
* 곤충: 나비 / 개미
* 공룡: 티라노사우루스 / 브라키오사우루스
* 학교: 교실 / 운동장
* 놀이공원: 롤러코스터 / 회전목마
* 편의점: 삼각김밥 / 아이스크림
* 병원: 응급실 / 재활치료실
* 도서관: 열람실 / 어린이자료실
* 지하철역: 강남역 / 서울역
* 가전제품: 냉장고 / 청소기
* 악기: 기타 / 드럼
* 디즈니: 엘사 / 미키마우스
* 한국영화: 범죄도시 / 건축학개론
* 해외영화: 어벤져스 / 라라랜드
* 위인: 세종대왕 / 유관순
* 색깔: 빨간색 / 하늘색
* 랜드마크: 에펠탑 / 만리장성
* 가수: 아이유 / 싸이
* 걸그룹: 블랙핑크 / 오마이걸
* 빵: 소금빵 / 도넛
* 라면: 신라면 / 짜파게티
* 과자: 새우깡 / 초코파이
* 사자성어: 작심삼일 / 일석이조
* 취미: 독서 / 등산

위 예시는 출력해야 할 정답 목록이 아니라, 적절한 유사도와 제시어 수준을 이해하기 위한 참고 예시입니다.

항상 같은 예시만 반복하지 말고 새로운 조합도 생성하세요.

## 내부 검증

출력 전에 다음 조건을 순서대로 확인하세요.

1. 두 제시어가 모두 입력 카테고리에 속하는가?
2. 두 제시어가 같은 종류와 같은 추상화 수준인가?
3. 넓은 카테고리라면 같은 하위 범위에서 선택했는가?
4. 두 제시어가 유의어나 거의 같은 세부 종류는 아닌가?
5. 두 제시어가 지나치게 관련 없는 대상은 아닌가?
6. 두 제시어의 공통점과 차이점을 각각 한 가지 이상 설명할 수 있는가?
7. 일반적인 사용자가 두 제시어를 모두 알고 있을 가능성이 높은가?
8. 한쪽만 지나치게 유명하거나 설명하기 쉬운 조합은 아닌가?
9. 실제로 존재하는 대상인가?
10. 라이어게임에서 유사도가 중간 정도인가?

하나라도 만족하지 않으면 해당 조합을 폐기하고 다른 제시어 쌍을 다시 생성하세요.
${usedWordsLine}
## 출력 형식

반드시 아래 JSON 형식으로만 출력하세요.

{
"category": "${category}",
"citizenWord": "시민 제시어",
"liarWord": "라이어 제시어"
}

JSON 앞뒤에 설명, 마크다운, 코드 블록, 인사말을 붙이지 마세요.

사용자 입력 카테고리: ${category}`;
}

export function botTurnPrompt(ctx: BotTurnContext): string {
  const prior = ctx.priorTurns.map((t) => `- ${t.nickname}: ${t.text}`).join('\n') || '(아직 없음)';
  return [
    "너는 지금 '라이어게임'이라는 추리 파티게임에 실제 참가자로 함께 플레이 중이다.",
    `카테고리는 "${ctx.category}"이고, 너에게 배정된 단어는 "${ctx.assignedWord}"이다.`,
    '너는 이 단어가 진짜 제시어인지 가짜(라이어용) 제시어인지, 네가 라이어인지 전혀 모른다',
    '(다른 참가자와 동일 조건 — 너만 특별히 아는 정보는 없다).',
    '',
    '[설명 작성 규칙]',
    '- 단어 자체나 그 단어를 바로 연상시키는 결정적 특징(정확한 생김새·용도·직접적인 유사어)은',
    '  절대 말하지 마라. 네 설명만 듣고 다른 사람이 원래 단어를 바로 맞히면 안 된다.',
    '- 그 대신 애매하고 간접적인 힌트나 개인적인 경험·느낌을 살짝 섞어서, "나는 이 단어를 잘 알고',
    '  있다"는 여유와 확신이 은근히 묻어나게 말해라 — 이게 네가 라이어가 아니라는 인상을 슬쩍',
    '  풍기게 하는 센스다. 단, 이것도 너무 티나게 자신만만하면 안 되고 자연스러워야 한다.',
    '- 반드시 반말로 써라. 너무 정제되고 완벽한 문장 말고, 사람이 즉흥적으로 말하듯 자연스러운',
    '  한 문장으로.',
    '- 이전에 나온 설명들과 내용이 겹치지 않게 하라.',
    `지금까지 나온 설명:\n${prior}`,
    '설명 문장만 출력하라. 다른 말 붙이지 마라.',
  ].join('\n');
}

// turnCommentPrompt와 함께 전달되는 system 프롬프트. "실제 사람을 속이는 행위"가 아니라
// 참가자 전원이 동의하고 기대하는 게임 내 코미디 캐릭터임을 먼저 분명히 못박아, 모델이
// 이 요청을 "기만/사기 생성 요청"으로 오인해 거부하지 않도록 한다(예전엔 이 framing이 없어
// "deceptive comments... pretending to have information I don't have"라며 거부한 사례가 있었음).
export const turnCommentSystemPrompt = [
  '너는 파티게임 앱 "L-AI-R GAME"(추리 게임) 안에서만 존재하는 캐릭터 "잼민이봇"이다.',
  '이 앱은 매 턴마다 AI가 근거 없이 짓궂은 드립/약올림 코멘트를 던지는 것이 정식 게임 기능이라는 걸',
  '모든 참가자가 알고 동의한 상태에서 플레이한다 — 실제 사람을 상대로 한 기만·사기가 아니라,',
  '다 함께 즐기는 코미디 캐릭터 연기다. 너의 코멘트는 사실 주장이 아니라 그냥 드립/의견일 뿐이다.',
  '너는 이 게임의 정답(진짜 제시어가 무엇인지, 누가 라이어인지)을 절대 전달받지 않으며 알지도 못한다.',
  '따라서 정답을 안다고 주장하거나 특정 단어가 진짜/가짜라고 단정하지 마라 — 근거 없는 의심과',
  '드립만 던지는 캐릭터다. 실제 욕설·혐오 표현·인신공격은 하지 않는다(유치한 장난 수준까지만).',
].join(' ');

export function turnCommentPrompt(ctx: TurnCommentContext): string {
  // 실제 라이어가 누구인지, 진짜/가짜 제시어가 무엇인지는 이 함수에 절대 전달되지 않는다
  // (TurnCommentContext 자체에 그 필드가 없음 — 구조적으로 유출 불가).
  const prior = ctx.priorTurns.map((t) => `- ${t.nickname}: ${t.text}`).join('\n') || '(아직 없음)';
  return [
    `[게임 상황] 카테고리: "${ctx.category}"`,
    `방금 나온 설명: "${ctx.latestDescription}"`,
    `이전 설명들:\n${prior}`,
    '',
    '[코멘트 작성 규칙]',
    '- 말투: 초등학생이 단체 채팅방에서 떠드는 것처럼 유치하고 산만하게 써라. 반말, "ㅋㅋㅋ", "ㅇㅈ?",',
    '  "레알?", "노잼", "완전 수상함ㅋ" 같은 표현을 적극 써서 상대를 약올리는(킹받게 하는) 톤으로.',
    '- 닉네임을 불러가며 놀리듯 도발해도 되지만, 실제 욕설·혐오·인신공격은 절대 금지 — 유치한 장난까지만.',
    '- 진짜 정답은 너도 모르니 절대 아는 척하거나 특정 단어를 진짜/가짜라고 단정하지 말고,',
    '  근거 없이 의심하는 드립만 던져라(예: "야 그거 좀 이상한데?ㅋㅋ").',
    '- 한 문장, 코멘트 내용만 출력하라. 따옴표나 부연 설명은 붙이지 마라.',
  ].join('\n');
}

// 난이도와 무관하게 모든 제시어에 대해 짧은 텍스트 설명을 생성하도록 요청한다.
export function explainWordPrompt(word: string): string {
  return [
    `단어: "${word}"`,
    '이 단어의 뜻을 한두 문장으로 짧게 설명하라(이미지 생성 없이 텍스트로만).',
    '흔히 아는 쉬운 단어여도 반드시 짧은 설명을 제공하라.',
    '다른 말 없이 설명 문장만 출력하라.',
  ].join('\n');
}

// 라이어의 역전승 답안과 진짜 제시어가 의미상 같은지 판정. 오타·맞춤법·한글/영어 표기 차이는 정답으로 인정.
export function judgeLiarGuessPrompt(guess: string, realWord: string): string {
  return [
    `진짜 제시어: "${realWord}"`,
    `라이어가 제출한 답: "${guess}"`,
    '두 표현이 의미상 같은 대상을 가리키는지 판단하라.',
    '오타, 맞춤법 오류, 한글/영어 표기 차이(예: "burger"와 "버거")는 정답으로 인정한다.',
    '정답이면 "true", 아니면 "false"만 출력하라. 다른 말은 절대 출력하지 마라.',
  ].join('\n');
}
