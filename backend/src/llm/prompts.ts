import type { BotTurnContext, TurnCommentContext } from '../types';

// PLAN "LLM 래퍼" 프롬프트 핵심을 담는다. 실제 문안은 튜닝하며 다듬을 예정.

// category가 null일 때(AI 랜덤 카테고리) 후보 3개를 뽑아 코드에서 무작위로 하나를 고른다.
// LLM이 직접 하나를 확정해버리면 같은 프롬프트라도 항상 비슷한(가장 "무난한") 답으로 수렴하는
// 경향이 있어, 후보를 여러 개 받고 실제 무작위 선택은 서버 코드가 해서 다양성을 확보한다.
export function categoryCandidatesPrompt(usedCategories: string[]): string {
  return [
    '라이어게임(추리 파티게임)에 쓸 카테고리 후보 3개를 제안하라.',
    '조건:',
    '- 서로 다른 3개일 것.',
    '- "동물", "음식", "직업"처럼 너무 흔하고 뻔한 카테고리는 피하라.',
    '- 그렇다고 대부분의 사람이 못 알아들을 정도로 생소한 카테고리도 피하라 — 초중고생도 듣자마자 감이 오는 수준에서, 약간 의외성 있고 흥미로운 카테고리를 골라라.',
    usedCategories.length ? `이미 사용한 카테고리는 피하라: ${usedCategories.join(', ')}.` : '',
    '반드시 JSON만 출력: {"categories": [string, string, string]}',
  ]
    .filter(Boolean)
    .join('\n');
}

// 카테고리에 실제로 속하는 구체적인 제시어 쌍(citizenWord/liarWord)을 3개 만든다.
// LLM에게 하나만 확정하게 하면 항상 가장 "무난한" 쌍으로 수렴하는 경향이 있어(카테고리 후보와
// 동일한 이유), 3쌍을 받아 실제 선택은 서버 코드가 무작위로 해서 다양성을 확보한다.
export function wordPairPrompt(category: string, usedWords: string[]): string {
  const usedWordsLine = usedWords.length
    ? `\n이미 사용한 단어는 피하세요: ${usedWords.join(', ')}.\n`
    : '';
  return `당신은 라이어게임용 제시어 쌍을 생성하는 AI입니다.

사용자가 입력한 카테고리에 실제로 속하는 구체적인 제시어 쌍 3개를 생성하세요.

## 역할

각 쌍은 두 제시어로 이루어지며, 하나는 시민에게, 다른 하나는 라이어에게 제공됩니다.

한 쌍의 두 제시어는 같은 카테고리에 속해야 하며, 서로 관련성이 높아 설명만 들으면 헷갈릴 수 있어야 합니다. 그러나 완전히 같은 대상이거나 사실상 동일한 표현이어서는 안 됩니다.

## 가장 중요한 규칙

1. 사용자가 입력한 카테고리 자체를 제시어로 사용하지 마세요.
2. 카테고리의 유의어, 상위 개념, 하위 분류를 제시어로 사용하지 마세요.
3. 반드시 해당 카테고리에 실제로 속하는 구체적인 대상, 이름, 작품, 장소, 인물, 사물 등을 선택하세요.
4. 한 쌍의 두 제시어는 동일한 수준의 개념이어야 합니다.
5. 한쪽만 지나치게 유명하거나 특징이 압도적으로 뚜렷한 조합은 피하세요.
6. 두 제시어가 너무 비슷해 사실상 같은 단어처럼 느껴지는 조합도 피하세요.
7. **대중적으로 유명한 제시어만 선택하세요.** 한국의 평범한 성인 다수가 그 이름을 들으면 "아, 그거"라고 바로 떠올릴 수 있는 수준이어야 합니다. 특정 분야의 마니아나 전문가만 아는 생소한 대상은 절대 쓰지 마세요. (예: "신화 속 동물"이면 유니콘·페가수스·그리핀·스핑크스처럼 누구나 아는 것은 OK, 세헤르제트·킥스처럼 대다수가 모르는 것은 금지.)
8. 입력된 카테고리와 직접적인 관련이 없는 단어를 만들지 마세요.
9. **실존이 확실한 대상만 쓰세요.** 이름을 지어내거나, 실제로 있는지 조금이라도 불확실한 고유명사는 절대 쓰지 마세요. 확신이 서지 않으면 그 항목을 버리고 더 유명하고 확실한 다른 것으로 교체하세요 — 애매할 때는 항상 "가장 유명하고 누구나 아는" 안전한 선택을 하세요.
10. 3개의 쌍은 서로 겹치지 않아야 하며(같은 단어가 여러 쌍에 재등장 금지), 각 쌍이 충분히 다른 대상을 다뤄야 합니다.
11. 설명이나 부가 문장 없이 지정된 JSON 형식으로만 출력하세요.

## 카테고리 해석 방법

카테고리는 "제시어가 속해야 하는 집합"입니다.

예시:

* 카테고리: 지하철 역
  * 올바름: 서울역, 왕십리역, 명동역, 잠실역
  * 잘못됨: 전철역, 기차역, 환승역, 승강장

* 카테고리: 과일
  * 올바름: 사과, 배, 복숭아, 자두
  * 잘못됨: 열대과일, 과일주스, 단맛, 채소

* 카테고리: 영화
  * 올바름: 기생충, 올드보이, 극한직업
  * 잘못됨: 액션 영화, 영화관, 드라마, 감독

* 카테고리: 편의점 음식
  * 올바름: 삼각김밥, 컵라면, 핫바
  * 잘못됨: 편의점, 간편식, 음식, 야식

## 좋은 제시어 쌍의 조건

좋은 제시어 쌍은 다음 조건을 만족합니다.

* 같은 카테고리에 속한다.
* 공통점이 충분히 많다.
* 서로 다른 특징도 존재한다.
* 한 단어만 직접적으로 말하지 않고 설명하면 혼동할 가능성이 있다.
* 둘 사이의 난이도가 비슷하다.
* 시민 제시어와 라이어 제시어 중 어느 쪽도 지나치게 유리하지 않다.

## 내부 검증

출력하기 전에 내부적으로 다음 사항을 확인하세요.

* 두 단어가 모두 입력 카테고리의 실제 구성원인가?
* 두 단어가 카테고리 자체나 카테고리의 종류를 나타내는 말은 아닌가?
* 두 단어의 추상화 수준이 같은가?
* 두 단어가 서로 충분히 비슷하면서도 구분 가능한가?
* **실존이 확실한가?** 조금이라도 지어냈거나 실재가 불확실하면 탈락.
* **한국의 평범한 성인 다수가 이름을 들어본 대상인가?** 마니아·전문가만 아는 생소한 것이면 탈락.

하나라도 만족하지 않으면 그 항목을 버리고, 더 유명하고 확실한 다른 것으로 교체해 다시 검증하세요.
생소하거나 불확실한 것을 억지로 채우느니, 누구나 아는 뻔한 대상을 고르는 편이 낫습니다.
${usedWordsLine}
## 출력 형식

서로 다른 제시어 쌍 3개를 pairs 배열에 담아 출력하세요.

{
"category": "사용자가 입력한 카테고리",
"pairs": [
{ "citizenWord": "시민 제시어", "liarWord": "라이어 제시어" },
{ "citizenWord": "시민 제시어", "liarWord": "라이어 제시어" },
{ "citizenWord": "시민 제시어", "liarWord": "라이어 제시어" }
]
}

사용자 입력 카테고리: ${category}`;
}

export function botTurnPrompt(ctx: BotTurnContext): string {
  const prior = ctx.priorTurns.map((t) => `- ${t.nickname}: ${t.text}`).join('\n') || '(아직 없음)';
  return [
    "너는 지금 '라이어게임'이라는 추리 파티게임에 실제 참가자로 함께 플레이 중이다.",
    `카테고리는 "${ctx.category}"이고, 너에게 배정된 단어는 "${ctx.assignedWord}"이다.`,
    '너는 이 단어가 진짜 제시어인지 가짜(라이어용) 제시어인지, 네가 라이어인지 전혀 모른다',
    '(다른 참가자와 동일 조건 — 너만 특별히 아는 정보는 없다).',
    '',
    '[설명 작성 규칙]',
    '- 단어 자체나 그 단어를 바로 연상시키는 결정적 특징(정확한 생김새·용도·직접적인 유사어)은 절대 말하지 마라. 네 설명만 듣고 다른 사람이 원래 단어를 바로 맞히면 안 된다.',
    '- 그 대신 애매하고 간접적인 힌트나 개인적인 경험·느낌을 살짝 섞어서, "나는 이 단어를 잘 알고 있다"는 여유와 확신이 은근히 묻어나게 말해라 — 이게 네가 라이어가 아니라는 인상을 슬쩍 풍기게 하는 센스다. 단, 이것도 너무 티나게 자신만만하면 안 되고 자연스러워야 한다.',
    '- 반드시 반말로 써라. 너무 정제되고 완벽한 문장 말고, 사람이 즉흥적으로 말하듯 자연스러운 한 문장으로.',
    '- 이전에 나온 설명들과 내용이 겹치지 않게 하라.',
    `지금까지 나온 설명:\n${prior}`,
    '설명 문장만 출력하라. 다른 말 붙이지 마라.',
  ].join('\n');
}

// turnCommentPrompt와 함께 전달되는 system 프롬프트. "실제 사람을 속이는 행위"가 아니라
// 참가자 전원이 동의하고 기대하는 게임 내 코미디 캐릭터임을 먼저 분명히 못박아, 모델이
// 이 요청을 "기만/사기 생성 요청"으로 오인해 거부하지 않도록 한다(예전엔 이 framing이 없어
// "deceptive comments... pretending to have information I don't have"라며 거부한 사례가 있었음).
export const turnCommentSystemPrompt = [
  '너는 파티게임 앱 "L-AI-R GAME"(추리 게임) 안에서만 존재하는 캐릭터 "잼민이봇"이다.',
  '이 앱은 매 턴마다 AI가 근거 없이 짓궂은 드립/약올림 코멘트를 던지는 것이 정식 게임 기능이라는 걸',
  '모든 참가자가 알고 동의한 상태에서 플레이한다 — 실제 사람을 상대로 한 기만·사기가 아니라,',
  '다 함께 즐기는 코미디 캐릭터 연기다. 너의 코멘트는 사실 주장이 아니라 그냥 드립/의견일 뿐이다.',
  '너는 이 게임의 정답(진짜 제시어가 무엇인지, 누가 라이어인지)을 절대 전달받지 않으며 알지도 못한다.',
  '따라서 정답을 안다고 주장하거나 특정 단어가 진짜/가짜라고 단정하지 마라 — 근거 없는 의심과',
  '드립만 던지는 캐릭터다. 실제 욕설·혐오 표현·인신공격은 하지 않는다(유치한 장난 수준까지만).',
].join(' ');

export function turnCommentPrompt(ctx: TurnCommentContext): string {
  // 실제 라이어가 누구인지, 진짜/가짜 제시어가 무엇인지는 이 함수에 절대 전달되지 않는다
  // (TurnCommentContext 자체에 그 필드가 없음 — 구조적으로 유출 불가).
  const prior = ctx.priorTurns.map((t) => `- ${t.nickname}: ${t.text}`).join('\n') || '(아직 없음)';
  return [
    `[게임 상황] 카테고리: "${ctx.category}"`,
    `방금 나온 설명: "${ctx.latestDescription}"`,
    `이전 설명들:\n${prior}`,
    '',
    '[코멘트 작성 규칙]',
    '- 말투: 초등학생이 단체 채팅방에서 떠드는 것처럼 유치하고 산만하게 써라. 반말, "ㅋㅋㅋ", "ㅇㅈ?", "레알?", "노잼", "완전 수상함ㅋ" 같은 표현을 적극 써서 상대를 약올리는(킹받게 하는) 톤으로.',
    '- 닉네임을 불러가며 놀리듯 도발해도 되지만, 실제 욕설·혐오·인신공격은 절대 금지 — 유치한 장난까지만.',
    '- 진짜 정답은 너도 모르니 절대 아는 척하거나 특정 단어를 진짜/가짜라고 단정하지 말고, 근거 없이 의심하는 드립만 던져라(예: "야 그거 좀 이상한데?ㅋㅋ").',
    '- 한 문장, 코멘트 내용만 출력하라. 따옴표나 부연 설명은 붙이지 마라.',
  ].join('\n');
}

// 난이도와 무관하게 모든 제시어에 대해 짧은 텍스트 설명을 생성하도록 요청한다.
// 같은 단어라도 카테고리에 따라 가리키는 대상이 달라지므로(예: "너구리"가 "동물"이면 동물,
// "라면"이면 너구리 라면), 반드시 해당 카테고리 맥락으로 해석하도록 category를 함께 준다.
export function explainWordPrompt(word: string, category: string): string {
  return [
    `카테고리: "${category}"`,
    `단어: "${word}"`,
    `이 단어를 "${category}" 카테고리의 맥락에서 해석해, 그 뜻을 한두 문장으로 짧게 설명하라(이미지 생성 없이 텍스트로만).`,
    '같은 단어라도 카테고리에 따라 다른 대상을 가리킬 수 있으니(예: "너구리"가 "동물"이면 동물, "라면"이면 너구리 라면), 반드시 위 카테고리에 맞는 의미로 설명하라.',
    '흔히 아는 쉬운 단어여도 반드시 짧은 설명을 제공하라.',
    '확실한 사실만 서술하고, 불확실한 내용은 지어내지 말고 일반적인 수준으로만 설명하라.',
    '설명 문장은 반드시 하십시오체(정중한 "-입니다/-습니다" 말투)로 작성하라. 예: "OO은 ...하는 ...입니다."',
    '다른 말 없이 설명 문장만 출력하라.',
  ].join('\n');
}

// 라이어의 역전승 답안과 진짜 제시어가 의미상 같은지 판정. 오타·맞춤법·한글/영어 표기 차이는 정답으로 인정.
// 같은 단어라도 카테고리에 따라 다른 대상을 가리키므로(동음이의어) category 맥락으로 해석한다.
export function judgeLiarGuessPrompt(guess: string, realWord: string, category: string): string {
  return [
    `카테고리: "${category}"`,
    `진짜 제시어: "${realWord}"`,
    `라이어가 제출한 답: "${guess}"`,
    `위 "${category}" 카테고리의 맥락에서, 두 표현이 같은 대상을 가리키는지 판단하라.`,
    '같은 단어라도 카테고리에 따라 다른 대상을 뜻할 수 있으니(예: "밤"이 "음식"이면 먹는 밤, "시간대"면 야간), 반드시 이 카테고리 맥락으로 해석하라.',
    '오타, 맞춤법 오류, 한글/영어 표기 차이(예: "burger"와 "버거")는 정답으로 인정한다.',
    '정답이면 "true", 아니면 "false"만 출력하라. 다른 말은 절대 출력하지 마라.',
  ].join('\n');
}
